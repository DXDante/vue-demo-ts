/** 接口
 *  interface {
 *      readonly xx (表示属性只读)
 *      xx? (表示可有可无属性)
 *  }
 */


/** 生物接口
 */
export interface Biology {

}


export interface Human {
    gender: string
    name: string
    age: number
    height: number
    weight: number
}